# Designo backend — FastAPI + SQLite + Claude generation
# Target: Python 3.12

fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
python-dotenv>=1.0
pydantic>=2.6

# Claude (site generation)
anthropic>=0.40

# fal.ai queue API + video download
httpx>=0.27

# Image validation / thumbnails
Pillow>=10.0

# Lead-gen: headless Chromium for email preview media (hero screenshot + scroll GIF)
# After install run: playwright install chromium --with-deps
playwright>=1.60
